name: aulaaplicacaonuvem-pipeline
 
on:
  push:
    branches: [ "main" ]
 
jobs:
  marketplace:
    runs-on: ubuntu-latest
    steps:
      - name: Example of use of marketplace
        uses: actions/setup-node@v2
        with:
          node-version: '14'
      - name: shows node version configured.
        run: node --version
