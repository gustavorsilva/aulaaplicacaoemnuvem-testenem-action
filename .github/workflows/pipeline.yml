name: Chamada do Workflow Integracao ECR/EB

on:
  push:
    branches:
      - main
      - feature/*

jobs:
  call-integracao:
    uses: gustavorsilva/modulesproject/.github/workflows/integracao-ecr-eb.yml@v1
    with:
      # Se você definiu inputs, passe os valores necessários
      branch: main
    secrets:
      S_GITHUB_TOKEN: ${{ secrets.S_GITHUB_TOKEN }}